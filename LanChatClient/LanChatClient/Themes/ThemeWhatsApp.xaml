<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- =========================================================
         LanChat Theme: WhatsApp-like Light (single source of truth)
         Guarantees stable keys used by UI:
         BrushBackground / BrushPrimary / BrushPrimaryDark
         + extra keys for future UI consistency.
         ========================================================= -->

    <!-- Core palette -->
    <Color x:Key="ColorPrimary">#FF128C7E</Color>
    <Color x:Key="ColorPrimaryDark">#FF075E54</Color>
    <Color x:Key="ColorPrimarySoft">#FF25D366</Color>

    <Color x:Key="ColorBackground">#FFF5F7FA</Color>
    <Color x:Key="ColorPanel">#FFFFFFFF</Color>
    <Color x:Key="ColorBorder">#FFDBE0E5</Color>

    <Color x:Key="ColorText">#FF1F2A33</Color>
    <Color x:Key="ColorSubtleText">#FF667781</Color>

    <!-- Chat specific -->
    <Color x:Key="ColorChatPaper">#FFEFEAE2</Color>
    <Color x:Key="ColorBubbleIncoming">#FFFFFFFF</Color>
    <Color x:Key="ColorBubbleOutgoing">#FFE6F4EA</Color>

    <!-- Brushes (stable keys) -->
    <SolidColorBrush x:Key="BrushPrimary" Color="{StaticResource ColorPrimary}"/>
    <SolidColorBrush x:Key="BrushPrimaryDark" Color="{StaticResource ColorPrimaryDark}"/>
    <SolidColorBrush x:Key="BrushPrimarySoft" Color="{StaticResource ColorPrimarySoft}"/>

    <SolidColorBrush x:Key="BrushBackground" Color="{StaticResource ColorBackground}"/>
    <SolidColorBrush x:Key="BrushPanel" Color="{StaticResource ColorPanel}"/>
    <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource ColorBorder}"/>

    <SolidColorBrush x:Key="BrushText" Color="{StaticResource ColorText}"/>
    <SolidColorBrush x:Key="BrushSubtleText" Color="{StaticResource ColorSubtleText}"/>

    <SolidColorBrush x:Key="BrushChatPaper" Color="{StaticResource ColorChatPaper}"/>
    <SolidColorBrush x:Key="BrushIncomingBubble" Color="{StaticResource ColorBubbleIncoming}"/>
    <SolidColorBrush x:Key="BrushOutgoingBubble" Color="{StaticResource ColorBubbleOutgoing}"/>

    <!-- Common effects -->
    <DropShadowEffect x:Key="ShadowSoft"
                      Color="#000000"
                      Opacity="0.10"
                      BlurRadius="14"
                      ShadowDepth="2"/>

    <!-- =========================================================
         Global base styles
         (TemplateBindings used so per-control overrides still work)
         ========================================================= -->

    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{StaticResource BrushBackground}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
    </Style>

    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{StaticResource BrushPrimary}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,7"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.92"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="0.82"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{StaticResource BrushText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="0"
                                      VerticalAlignment="Stretch"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ListBox}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

</ResourceDictionary>
